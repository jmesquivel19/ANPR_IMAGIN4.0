<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.esquivel.anpr_imagin" >
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.NoTitleBar" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
o        
        </activity>
        <activity
            android:name=".EntradaActivity"
            android:label="@string/title_activity_entrada" >
        </activity>

        <uses-permission android:name="android.permission.INTERNET"/>
        <uses-permission android:name="android.permission.CAMERA" />
        <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
w        <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
        <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

        <activity
            android:name=".SalidaActivity"
            android:label="@string/title_acivity_salida" >
        </activity>
        <activity
            android:name=".infoEntrada"
            android:label="@string/title_activity_info_entrada" >
        </activity>
        <activity
            android:name=".infoSalida"
            android:label="@string/title_activity_info_salida" >
        </activity>
    </application>

</manifest>
